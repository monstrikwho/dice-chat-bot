const fs = require("fs");
const axios = require("axios");
const cheerio = require("cheerio");
const querystring = require("querystring");
const nodeHtmlToImage = require("node-html-to-image");

const requestToday = (id) => {
  return id
}

module.exports = requestToday