{
  "name": "socketio-react-app",
  "version": "0.0.1",
  "description": "socket.io react app",
  "dependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "2.0.7"
  },
  "scripts": {
    "start": "concurrently \"npm run client\" \"npm run server\"",
    "client": "cd client && npm run start",
    "server": "cd server && nodemon server.js"
  }
}
